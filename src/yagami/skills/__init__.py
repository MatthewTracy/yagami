"""Skills are tools the LLM can call mid-turn (calc.eval, web.fetch, etc.).

Parallel to Backends. The Backend protocol describes "where the LLM runs";
the Skill protocol describes "what the LLM can invoke from within its run."
"""
